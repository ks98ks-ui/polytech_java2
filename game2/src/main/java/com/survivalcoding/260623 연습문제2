1번 val obj : X = A() ?
obj.a 만 호출가능 그 이유는 X에서 A만 선언되어 있기 때문에

2번답
Aa, Ba

A의 a가 Aa 출력하고 B의 a가 Ba를 출력하기 때문에

3번답

List는 Y를 공통으로 가지고 있기에 Y타입으로 만들어야함

list: List<Y> = listOf(A(), B())
for (obj in list) {
    obj.b()
}